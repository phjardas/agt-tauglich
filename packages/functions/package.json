{
  "name": "@agt-tauglich/functions",
  "private": true,
  "main": "lib/index.js",
  "scripts": {
    "build": "esbuild --bundle --platform=node --target=node22 --outdir=lib src/index.ts"
  },
  "dependencies": {
    "@agt-tauglich/model": "*",
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^6.0.1"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
