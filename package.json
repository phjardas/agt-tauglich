{
  "name": "@agt-tauglich/parent",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean": "shx rm -rf packages/*/{lib,dist} packages/*/tsconfig.tsbuildinfo && npm run --workspaces --if-present clean",
    "build": "npm run clean && tsc -b tsconfig.build.json && npm run --workspaces --if-present build",
    "test": "npm run --workspaces --if-present test",
    "start": "npm run --workspaces --if-present start"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "shx": "^0.3.4",
    "typescript": "^5.6.2"
  },
  "volta": {
    "node": "21.7.3"
  }
}
